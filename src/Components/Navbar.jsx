import React from "react";
import logo from "../images/Vector.svg";

const Navbar = () => {
   return (
      <header>
         <img src={logo} alt="" />
      </header>
   );
};

export default Navbar;
